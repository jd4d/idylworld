import styled from "styled-components";

// components
import { FullscreenContainer } from "../_shared/styled.js";

export const OrangeFullscreen = styled(FullscreenContainer)`
    background: #f5f5f5;
    margin-top: 65px;
`;
